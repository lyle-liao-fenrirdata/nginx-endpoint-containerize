version: "3"

name: nginx-endpoint
services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    image: nginx-endpoint
    restart: always
    ports:
      - "80:80"
      - "443:443"